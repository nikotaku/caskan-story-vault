# Repository workflow

- When the user asks to change this site, complete the requested implementation and relevant checks.
- Unless the user explicitly asks for local-only work, finish by pushing the change to GitHub, merging it into `main`, and confirming the resulting Vercel production deployment.
- Do not report completion until the merge and production deployment have both reached a successful state. If either is blocked, report the exact blocker.
